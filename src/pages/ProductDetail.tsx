import React from 'react';

const ProductDetail = () => {
    return (
        <div>
          {/* Contenido */}
          <h1>Detail</h1>
          {/* Elementos */}
        </div>
      );
};

export default ProductDetail;